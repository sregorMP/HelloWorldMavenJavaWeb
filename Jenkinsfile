pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'ls'
        sh 'echo "iniciando do build"'
      }
    }

    stage('Tests') {
      steps {
        sh 'pwd'
      }
    }
    stage('Deploy') {
      steos {
       sh 'whoami'
      } 

  }
}
}
